# 2026-09-11 安全核对与修复记录

## 核对范围

核对当前桌面客户端及信令服务器的本地源码，包括此前尚未发布的注册状态与 `voice-reconnect` 修改。外部报告引用的客户端 `f077b6a`、服务端 `3a810da` 均早于当前本地基线。

## 八项结论

| 编号 | 核对结论 | 处理结果 |
| --- | --- | --- |
| SEC-01 | 旧版本确实存在反代共享消息预算；当前基线 `c345904` 已移除 | 保留每条 WebSocket 独立的 120 条/10 秒预算；回归测试验证一个来源连接被限流不会影响另一连接 |
| SEC-02 | 仅有全局连接上限不足以防止单来源占满 | 分离 HTTP 握手池和已升级连接池；按真实来源限制并发，断开自动释放；IPv4 映射地址归一化，IPv6 按 /64 归并；只信任明确配置的反代地址 |
| SEC-03 | 密码参与大厅 ID 导致不同密码生成同名独立大厅，原密码错误分支无法正常触发 | 大厅 ID 改为名称哈希；在同一写锁内检查已有大厅密码后才允许加入；错误密码不能获得令牌或建立同名大厅 |
| SEC-04 | Unix 公共临时文件、缺少提权端内容校验的问题存在 | 移除临时文件和 `pkexec cp`；匿名管道传递固定请求，由受限 Helper 校验原文件哈希、MCTier 区域、虚拟 IP 和域名；固定 `/etc/hosts`，拒绝符号链接，在同一锁定描述符上检查与写入 |
| SEC-05 | MC 扫描、单个查询、延迟探测与 LAN 代理没有统一限制目标地址 | 共用严格 IPv4 字面量校验，只允许 `10.126.126.1` 到 `.254`；拒绝物理内网、回环、域名与 IPv6 绕过；扫描去重，代理列表有数量上限并拒绝零端口 |
| SEC-06 | 单次 Helper 的首个错误连接会直接失败；长会话握手读取也缺少完整的边界 | 两种调用共用有界认证接收器；错误令牌、超长前缀、断开的连接会被丢弃；静默连接单独过期，整体等待有绝对截止时间，可同时接受真正 Helper |
| SEC-07 | 原始 TCP 来源用于投稿冷却，反代下会碰撞；代码冷却是 30 秒而非报告所述 60 秒 | 投稿配额和冷却使用与连接防护相同的可信真实来源解析；不信任普通客户端自带的转发头 |
| SEC-08 | 发布源码不含 `.github` 时，测试仍无条件读取文件会失败；不能恢复该目录上传，因为用户要求不上传点开头目录 | CI 工作流存在时继续验证固定 action SHA 与凭据策略；不存在时明确跳过该项；Gradle 下载校验拆成独立测试，始终执行 |

另修复 hosts 结束标记被开始标记前缀错误匹配的问题，防止清理时误丢弃区域后方的其他记录。Windows 与 Unix 共享同一套内容校验。

## 验证

- 桌面端 `npm test`：114 项通过，包含前次注册/语音修复的测试。远控测试夹具已正确模拟注册成功，不绕过生产注册状态检查。
- 信令端 `cargo test --no-fail-fast`：56 项通过。包含真实本地 WebSocket 的错误密码、同名大厅、反代来源隔离、投稿冷却、跨大厅伪造、注册超时和语音重连测试。
- 桌面 Rust `cargo check --tests --locked`：通过。
- `cargo test --test native_security`（`src-tauri/tests/native_security.rs`，自 `tests/native-security.rs` 迁移：其引入的 `virtual_network.rs` 依赖 tokio/windows，原独立 `rustc` 单文件编译无法链接这些 crate）：5 项通过，实际执行 hosts 校验、虚拟网段校验与 TCP Helper 握手抢占/静默超时测试。
- Unix Helper 与共享校验模块：通过 `x86_64-unknown-linux-gnu` 目标的独立编译检查。隔离编译目录 `.security-check` 已被 Git 忽略，不属于发布源码。

## 部署要求与验证边界

客户端需要重新打包。服务端需要同步完整 `src`（含新增 `connection_guard.rs`）、Compose 和相关配置，重新构建并重建容器。只替换 `main.rs` 或只重启旧容器均不够。

反代部署必须设置 `TRUSTED_PROXIES` 为信令容器实际看到的代理 IP，并让边缘代理覆盖客户端自带的真实 IP 头。配置为空代表直连模式，不能区分反代后的用户。具体配置、端口收紧和更新命令见信令仓库 README 的“反向代理与 HTTPS/WSS”。

连接防护限制的是来源配额，不是用户账号。共享 NAT 或 IPv6 /64 下的用户仍共享来源额度，可按规模调整。应用层防护不能保证承受分布式攻击或带宽耗尽，公网边缘需要配套限制握手速率和抗 DDoS。

本轮未操作云端容器、未打包发布、未推送 GitHub，未对公网生产服务进行攻击测试。Windows UAC 与 Linux polkit 的完整授权交互未做实机验收；本机 WSL 因 VHDX 挂载权限错误无法启动，Linux 检查仅覆盖上述编译及跨平台共享逻辑测试。测试通过不等于对所有环境及未知漏洞作“绝无风险”的保证。
