- 修复桌面端创建大厅后房主身份和玩家列表未及时同步的问题，在发送注册请求前建立完整事件监听，并明确区分连接中、已注册和连接失败状态。
- 修复虚拟网卡启动较慢或端口尚未释放时，聊天服务初始化失败的问题，增加有时限的等待重试。
- 修复注册失败重试过程中本机聊天签名身份发生变化的问题，保留连接身份并及时撤销失效会话权限。
- 修复 Windows 日志查看等子进程继承聊天和文件服务套接字的问题，避免退出或重连后旧进程继续占用端口。
- 修复部分异常关闭使用无效 WebSocket 关闭码的问题，并优化注册失败后的连接清理和重试。
- 完成注册前禁用大厅邀请，避免其他设备加入尚未成功注册的大厅；迁移旧版官方信令地址，同时保留用户自定义端点。
- 修复部分 WebView2 环境对 EasyTier 节点地址的解析兼容问题，避免合法的 tcp、udp、txt 等节点地址因主机名解析差异被判定为无效。
- 修复节点选择器在当前值不存在时仍显示第一个节点的问题，使界面显示与实际提交的节点一致。
- 优化创建、加入大厅时的地址错误提示，分别指出 EasyTier 节点地址或信令服务器地址的问题，并在表单中保留错误信息。
- 优化大厅连接状态处理，仅在输入校验通过后进入连接中状态，避免地址无效时提前切换状态。
- 增加地址校验失败的脱敏诊断信息，便于区分普通节点、邀请、私有服务器和重试场景，不记录实际私有地址或密码。
- 修复文件共享密码失败记录达到容量上限后，其他合法新用户也会被拒绝访问的问题；记录满时淘汰最旧失败记录，保留过期清理、错误密码限制和内存上限。
- 收紧 Windows 聊天和文件共享服务的防火墙入站范围，限制本地及远端虚拟网地址，并为本地 Helper 保留回环通信；保留 EasyTier 公网组网及应用出站访问能力。
- 改进防火墙规则升级和检测，识别旧版宽范围规则，并在执行防火墙修复时替换为新版规则；修正 ICMP 回显规则参数。
- 移除 Windows 提权 Helper 命令行中的认证 Token，改为依据系统 TCP 连接所属进程验证 Helper 身份。
- 为提权 Helper 增加父进程身份和可执行文件路径校验，拒绝其他本地进程冒充原应用下发特权请求。
- 完善 Helper 身份查询重试、进程句柄生命周期和握手测试，覆盖进程信息暂不可用、非法连接、静默超时以及真实父子进程通信。
- 将原生安全测试迁移到 Cargo 集成测试目录，正确解析 Tokio、Windows 等依赖，并补充文件共享容量、防火墙访问范围及鉴权回归测试。
- 同步桌面端、Android 端、依赖锁文件及中英文 README 的 3.2.1 版本信息，Android versionCode 更新为 56。
