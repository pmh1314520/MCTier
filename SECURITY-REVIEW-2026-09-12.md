# 2026-09-12 安全修复与 PR 审查

审查基线：客户端 `2cce9015b417ababf84d80a081a49ca610c46390`，信令服务器 `32990c7875d0a42d6922f98119a26784aa19177e`。报告针对当前源码复核旧提交 `f077b6a` 的反馈，不将反馈中的攻击描述直接当作事实。

更新日志以仓库 `v3.2.0` 标签 `4fcf352` 为对比起点，包含该标签之后的 `2cce901` 注册稳定性修复及本次 3.2.1 改动；其中 `2cce901` 自身的版本元数据已是 3.2.0。

## 已修复的客户端问题

1. **文件共享失败记录容量导致误拒。** 旧代码已清理过期记录，接口也先验证大厅 Token、检查共享目录是否存在，来源 IP 来自 TCP 连接。因此不能仅靠伪造任意 share_id 或 HTTP 来源头直接填表。但当真实失败记录达到 4096 条时，旧实现确实在比较密码前拒绝所有未记录的键。现在有效的新访问不需要分配失败记录；新的错误访问在容量满时淘汰最近一次失败时间最早的记录。已有键的错误次数限制、过期窗口和成功清理保留。容量淘汰在可用性和有限内存之间取舍，不保证在持续轮换有效键的攻击下保留全部历史锁定。
2. **Windows 防火墙规则范围。** `profile=any` 本身不是问题，虚拟网卡可能属于任意 Windows 网络配置文件。原问题是应用入站和独立端口规则缺少地址范围。新应用入站规则限制虚拟子网和本地回环；TCP 14539、14540 同时限制虚拟子网两端和 MCTier 程序路径，ICMP 仅允许虚拟子网内的回显请求。EasyTier 的公网入站和出站、应用出站仍可用。新规则使用版本化名称，检查不再把旧规则视作已修复；实际替换发生在用户运行网络诊断的防火墙修复时。
3. **Helper 命令行凭据。** 原命令行含一次性 Token。现在只包含端口和父进程 PID；父进程持有提升进程句柄，通过 Windows TCP 属主表验证 Helper。Helper 在读取任何特权请求前，反向验证已连接服务端的 PID 和当前 MCTier 可执行文件路径。协议内保留固定类型的请求、现有 hosts 校验和 EasyTier 资源校验。

## PR 审查结论

### 客户端 PR #53：未合并

- 审查提交：`438cfbb8ebd26a6bae1dfe2b579afaaf9741425e`。
- 阻断问题：`src-tauri/src/modules/privileged_helper.rs:314` 的 Helper 只检查自身是否提升，连接指定本地端口后即接收请求，没有验证服务端所属父进程。如果原应用在授权/启动期间退出、端口被其他本地进程接管，提升后的 Helper 仍可能与接管进程通信。这是原机制已存在、该加固 PR 仍未解决的缺口，不应描述为该 PR 新引入的漏洞。
- PR 中的进程属主检查和测试迁移方向可用，本次主分支独立实现并补齐了双向验证及句柄生命周期处理；没有合并 PR 提交。
- 初审曾怀疑完整前缀后的空缓冲区读取必然导致断开。独立 Windows 复现测试未复现此断开，不能将其列作确认故障。本次实现避免空缓冲区读取，并用可注入查询覆盖暂不可用后恢复的行为。
- 原生进程身份测试验证了真实 Windows TCP 连接及同一测试程序的父子进程通信，没有实际批准 UAC、改写 hosts 或修改本机防火墙。

### 服务端 PR #5：未合并

- 审查提交：`80205d942e6adaae460596d1db71fa4bf2bfbe8a`。
- 阻断问题：`src/main.rs` 的 `RegisterPasswordFailures::is_locked` 在键不存在时，以全表容量达到 4096 作为拒绝条件；该检查位于查找/创建大厅及密码验证之前。因此大量失败记录可以使无关来源加入其他大厅，甚至创建全新大厅，也被返回“密码错误次数过多”。这是全局可用性回归。
- 该 PR 自带 62 项测试通过，但补充 `review_full_table_must_not_lock_unrelated_room_and_source` 后失败：填满失败表，再检查另一来源和无关大厅时被错误锁定。复现修改只放在本地隔离审查目录，没有进入服务端主分支。
- 新限流还会让共用同一公网 IPv4 或 IPv6 /64 的用户共用大厅密码锁定；五次误输即可临时阻止同来源的正确密码访问。发布前需要明确共享出口场景的策略。
- 盐和一致性校验不等于慢密码哈希，不能将单轮加盐 SHA-256 描述为完整的离线弱密码防护。反代仍须正确配置受信代理及覆写转发头。
- 服务端主分支保持 `32990c7`，本次不要求更新云端容器。

## 验证与发布边界

- `npm test`：127 项通过。
- `npm run build`：TypeScript 和 Vite 生产构建通过。
- `cargo check --release --locked`：通过，覆盖 release 专用的 Helper 启动和资源校验代码路径。
- `cargo test --lib --test native_security --locked --no-fail-fast`：195 项库测试、12 项原生安全测试通过，包含真实父子进程验证和数据交换。
- 改动涉及的两个 TypeScript 文件，ESLint 相对 HEAD 均未增加错误或警告；仍有原有 6 个错误和 35 个警告，不能声称 ESLint 全绿。
- 节点兼容性修复通过模拟旧 URL 解析行为和当前运行时验证，未取得发生报错用户的旧版 WebView2 实机复现。
- 本次未执行双端真人语音、远控或 UAC 授权后的完整操作验收；没有重新生成安装包或 APK。现有已打包文件不包含本次新安全修复，需重新打包。已安装的旧防火墙规则需通过新版的防火墙修复完成替换。
- E 盘空间不足时改用 C 盘独立 Cargo 构建缓存进行测试。直接删除增量缓存被自动审批拦截，随后通过 `cargo clean -p mctier --profile dev` 清理该包可再生成的调试产物释放空间；源码和发布包保留。不将构建目录、点开头目录、备份或 ZIP 包纳入 Git 提交。
