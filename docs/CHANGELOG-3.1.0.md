- 修复桌面端使用常用收藏大厅自动回填时，密码可能进入玩家名称输入框的问题；选择收藏时保留当前玩家名称。
- 修复收藏大厅管理表单与加入大厅表单的输入框 ID 冲突，减少自动填写时字段串位的问题。
- 兼容旧收藏中的异常昵称数据，清理与大厅密码相同的收藏昵称，避免错误数据继续影响回填。
- 优化桌面端信令初始化：等待服务端确认注册成功并完成本地聊天认证配置后，再启动依赖信令的后续功能。
- 修复桌面端在尚未完成注册时提前发送玩家列表、屏幕共享和远程控制等业务消息的问题，减少未注册请求被服务端拒绝造成的连接异常。
- 完善 Android 端发送检查，业务消息必须在注册成功且服务端会话编号有效时才能发送；打开新连接时清除旧连接的注册状态。
- 为桌面端完整注册流程增加 15 秒超时处理，覆盖连接、挑战签名、注册响应和本地认证配置阶段，避免初始化一直等待。
- 完善桌面端注册失败、连接提前关闭、手动退出和取消操作的收尾处理，及时终止尚未完成的注册流程。
- 加强桌面端连接切换保护，旧连接的延迟消息和过期注册响应不能启用新连接的业务发送权限。
- 修复桌面端聊天认证初始化失败后清除签名状态，却未在下一次注册重试时重新准备签名状态的问题；自动重连也会重新确认签名身份。
- 修复桌面端加入已有大厅时，玩家列表尚未到达就校验远端房主身份，可能导致聊天认证初始化失败的问题；初始阶段先安装本机认证凭据，随后根据成员列表应用房主身份。
- 改进桌面端大厅初始化错误提示：保留具体失败原因并显示“大厅连接未完成”提示，避免失败仅记录在控制台中。
- 补全桌面端与信令服务器的 voice-reconnect 消息处理，使指定玩家之间可以请求重新协商语音连接。
- 加强语音重连请求校验，只允许当前大厅内已认证玩家之间转发，并在桌面接收端核对发送者、接收者和当前会话编号。
- 优化桌面端手动语音重连：信令不可用、重连请求未能发送时，不提前拆除已有的对端连接。
- 修复双端私聊未读残留：确认发送者离开大厅后，自动清除该玩家私信的未读计数，私聊分页和聊天室入口提示同步更新；保留聊天记录、公共聊天未读和其他玩家的私信未读。
- 修复信令服务器在反向代理部署下，多个玩家共用同一个消息限流额度而被连带断开的问题；消息限速按每条连接独立计算。
- 移除原有按直接来源 IP 最多同时连接 8 次的限制，新增可配置的真实来源连接配额；默认每个来源最多 128 条连接，反代部署需配置可信代理和真实 IP 转发头。
- 新增信令服务器可信反代配置 TRUSTED_PROXIES，只采信指定上游代理提供的 X-Forwarded-For 或 X-Real-IP，避免普通客户端伪造转发头绕过来源限制。
- 新增独立的握手连接池和已建立连接配额，限制未完成握手的连接占用资源；IPv6 来源按 /64 网段归并计算配额。
- 修复反代环境下社区节点投稿冷却互相影响的问题，按经可信代理解析后的真实来源区分投稿配额与冷却。
- 修复输错已有大厅密码时可能创建同名“幽灵大厅”的问题；大厅标识只由名称决定，密码单独校验，错误时明确返回“密码错误”。
- 修复大厅名称和密码中的分隔符组合可能产生相同大厅标识的问题，防止不同名称与密码组合错误关联到同一个大厅。
- 改进旧客户端版本错误反馈，在拒绝不符合最低版本要求的旧注册请求时返回明确的版本提示。
- 加强桌面端 Minecraft 服务器查询、批量扫描、延迟探测和局域网广播代理的目标校验，只允许 10.126.126.1 至 10.126.126.254 的虚拟网段 IPv4 地址，拒绝物理内网、回环、域名和 IPv6 等越界目标。
- 优化 Minecraft 扫描和代理输入处理，对重复目标去重，限制代理列表数量并拒绝无效端口。
- 加固 Windows 特权 Helper 的认证握手，忽略错误令牌、异常数据和提前断开的连接；限制握手长度、等待时间和并发候选连接，减少本机异常连接抢占导致的提权失败。
- 加固 Linux 等 Unix 平台的 hosts 更新流程，通过匿名管道向固定的特权操作传递请求，移除公共临时文件中转，降低授权等待期间内容被替换的风险。
- 统一 Windows 与 Unix 的 hosts 内容校验，只允许修改 MCTier 管理区域及合法虚拟网段域名映射；Unix 特权端同时检查文件属性、内容快照并锁定文件描述符。
- 修复 hosts 清理时结束标记被误识别为开始标记的问题，避免误删 MCTier 区域后方的其他 hosts 记录。
- 修复源码不包含 .github 目录时供应链测试报错的问题；存在工作流时继续检查依赖固定规则，Gradle 下载校验独立执行。
- 补充收藏回填、注册重试、注册权限、语音重连、私聊未读、错误密码、可信代理和特权握手等回归测试，并更新安全审计与服务器部署说明。
- 将桌面端、Android 端及相关版本展示统一更新为 3.1.0，Android versionCode 更新为 51。
