const fs=require('fs');const s=fs.readFileSync('src/components/ChatRoom/ChatRoom.tsx','utf8');const m1='const [previewImage, setPreviewImage] = useState<string | null>(null);';const i1=s.indexOf(m1);const newS = i1 ? s.slice(0,i1+m1.length)+'\\n  const [previewZoom, setPreviewZoom] = useState(1);'+s.slice(i1+m1.length) : s;const m2='const fileInputRef = useRef<HTMLInputElement>(null);';const i2=newS.indexOf(m2);const newS2=i2 ? newS.slice(0,i2+m2.length)+'\\n  const initializedScrollRef = useRef(false);'+newS.slice(i2+m2.length) : newS;fs.writeFileSync('src/components/ChatRoom/ChatRoom.tsx',newS2.replace('import { MicrophoneIcon, VolumeIcon, PlayerIcon }','import { MicrophoneIcon, VolumeIcon }'),'utf8');
